# The service name, this will be part of the endpoint of the Prisma API
service: graphql-hackernews-node

# The deployment stage, this will also bepart of the API endpoint
stage: dev

# Points to the file that holds your data model
datamodel: datamodel.graphql

# You can only access the API when providing JWTs that are signed this secret
secret: mysecret123

cluster: local